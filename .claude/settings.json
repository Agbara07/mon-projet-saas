{
  "permissions": {
    "defaultMode": "acceptEdits",
    "allow": [
      "Bash(*)",
      "Edit(*)",
      "Write(*)",
      "Read(*)",
      "Glob(*)",
      "Grep(*)"
    ]
  },
  "mcpServers": {
    "codegraph": {
      "type": "stdio",
      "command": "codegraph",
      "args": ["serve", "--mcp"]
    },

    "firecrawl": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_KEY": "${FIRECRAWL_API_KEY}",
        "FIRECRAWL_RETRY_MAX_ATTEMPTS": "3",
        "FIRECRAWL_RETRY_INITIAL_DELAY": "1000",
        "FIRECRAWL_CREDIT_WARNING_THRESHOLD": "200",
        "FIRECRAWL_CREDIT_CRITICAL_THRESHOLD": "50"
      }
    },

    "gsc": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "suganthan-gsc-mcp"],
      "env": {
        "GOOGLE_APPLICATION_CREDENTIALS": "${GOOGLE_APPLICATION_CREDENTIALS}"
      }
    },

    "brightdata": {
      "type": "stdio",
      "command": "npx",
      "args": ["@brightdata/mcp"],
      "env": {
        "API_TOKEN": "${BRIGHTDATA_API_TOKEN}",
        "BROWSER_AUTH": "${BRIGHTDATA_BROWSER_AUTH}"
      }
    }
  },
  "hooks": {
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Avant que le contexte soit compressé, mets à jour la section '## Journal de session' dans le fichier CLAUDE.md à la racine du projet. Insère une nouvelle entrée horodatée (format: '### Session du JJ/MM/AAAA — HH:MM') qui résume en bullet points concis les actions effectuées durant cette session : fichiers créés ou modifiés, commandes exécutées, bugs corrigés, décisions techniques prises. Ne supprime pas les entrées existantes, ajoute la nouvelle juste après le commentaire HTML. Maximum 10 bullet points.",
            "statusMessage": "Sauvegarde de la session dans CLAUDE.md..."
          }
        ]
      }
    ]
  }
}
